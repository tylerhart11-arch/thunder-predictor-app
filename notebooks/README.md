# Notebooks

Use notebooks here for exploration, one-off diagnostics, and experimentation.

If notebook logic becomes part of the repeatable workflow, move it into `app/`, `pipelines/`, or `scripts/`.
